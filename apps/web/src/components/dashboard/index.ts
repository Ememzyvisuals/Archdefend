'use client';
// Re-exported stubs — actual implementations are inline in dashboard/page.tsx
// These exist so imports resolve. Components are rendered directly in the dashboard.
export function ScoreRadar() { return null; }
export function SecurityPanel() { return null; }
export function InterviewPanel() { return null; }
export function ExportPanel() { return null; }
